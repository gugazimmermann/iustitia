export interface ProfileListInterface {
  id: string;
  avatar: string;
  name: string;
  phone: string;
  email: string;
  role: string;
  active: boolean;
}

export interface SimpleProfileListInterface {
  id: string;
  name: string;
  avatar: string;
}
